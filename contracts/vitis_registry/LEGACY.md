# LEGACY — Soroban Contract

Este directorio contiene el contrato **Stellar Soroban** (`vitis_registry`) usado en versiones anteriores de VitisTrust.

**Contrato activo:** [`../VitisRegistry.sol`](../VitisRegistry.sol) (Rootstock / EVM).

No usar este crate para nuevos deploys. Para desplegar el registro on-chain actual, ver `scripts/deploy_rsk.py` y `DEPLOY.md`.
