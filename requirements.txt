# VitisTrust Backend Dependencies
# ================================
# Hedera (HCS) + Rootstock (EVM)

# FastAPI & Server
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-dotenv>=1.0.0
httpx>=0.26.0
requests>=2.28.0

# AI (Groq — llama-3.3-70b-versatile)
groq>=0.5.0

# Hedera SDK (Hiero) — PyPI latest is 0.2.x
hiero-sdk-python>=0.2.0

# Rootstock / EVM (web3; también usado por validation_agent)
web3>=6.11.0

# Utilities
pydantic>=2.5.0
pydantic-settings>=2.1.0
sse-starlette>=2.0.0

# Testing (opcional)
pytest>=7.4.0
pytest-asyncio>=0.23.0
